{
  "name": "omf-atlas",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1 --port 3016",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1 --port 3016",
    "test": "node --test tests/*.test.mjs"
  },
  "dependencies": {
    "three": "0.159.0"
  },
  "devDependencies": {
    "meshoptimizer": "^1.2.0",
    "occt-import-js": "^0.0.23",
    "vite": "8.2.2"
  }
}
